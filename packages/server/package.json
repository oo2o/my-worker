{
  "name": "mock-server",
  "description": "A mock server for homework",
  "scripts": {
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 3001"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}